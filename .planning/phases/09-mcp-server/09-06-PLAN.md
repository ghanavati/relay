---
phase: 09-mcp-server
plan: 06
type: execute
wave: 4
depends_on: ["09-05"]
files_modified:
  - docs/commands.md
  - docs/sessions/mcp.md
  - README.md
  - CHANGELOG.md
autonomous: false
requirements: [MCP-01, MCP-07]
user_setup: []

must_haves:
  truths:
    - "A user can find the `.mcp.json` registration entry for Claude Code / Claude Desktop and mount Relay as an MCP server from the docs"
    - "docs/commands.md documents `relay mcp` and the eight exposed tools (two memory + six session-control)"
    - "The README and CHANGELOG reflect Relay-as-MCP-server as a shipped capability, consistent with the control-layer positioning"
    - "A human verifies end-to-end that an MCP client enumerates Relay's tools and a memory recall round-trips over MCP"
    - "The full existing test suite is green and the `relay` CLI surface is unchanged (additive only) — the MCP-07 final gate"
  artifacts:
    - path: "docs/sessions/mcp.md"
      provides: "The `relay mcp` reference: what it exposes, the .mcp.json entry, security posture (caller-as-llm-session, default-deny, redaction, stdout discipline), and O-01/O-03 scope notes"
      min_lines: 40
    - path: "docs/commands.md"
      provides: "A `relay mcp` command section + the eight tool names"
      contains: "relay mcp"
  key_links:
    - from: "docs/sessions/mcp.md"
      to: ".mcp.json (user's client config)"
      via: "copy-paste registration block: command 'relay', args ['mcp']"
      pattern: "mcp"
    - from: "README.md"
      to: "the MCP server capability"
      via: "control-layer positioning mention + pointer to docs/sessions/mcp.md"
      pattern: "MCP"
---

<objective>
Document Relay-as-MCP-server and prove the whole phase end-to-end. Ship the `.mcp.json` registration recipe for Claude Code / Claude Desktop, document `relay mcp` and the eight exposed tools, update the README + CHANGELOG to reflect the shipped capability, then run the MCP-07 final gate: a human round-trips a tool call from a real MCP client and the full suite stays green with the CLI unchanged.

Purpose: MCP-01's success criterion is that a client registers Relay via `.mcp.json` and enumerates its tools — that is a documentation + verification deliverable, not just code. MCP-07's closing clause is "full existing test suite stays green and the relay CLI surface is unchanged (additive only)"; this plan is where that is formally checked across the assembled phase.

Output: docs/sessions/mcp.md (the reference), updated docs/commands.md, README.md, CHANGELOG.md, plus a human-verify checkpoint confirming the live round-trip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mcp-server/09-CONTEXT.md
@docs/commands.md
@README.md
@CHANGELOG.md
@.planning/phases/09-mcp-server/09-03-SUMMARY.md
@.planning/phases/09-mcp-server/09-04-SUMMARY.md
@.planning/phases/09-mcp-server/09-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the MCP server docs + .mcp.json registration (MCP-01)</name>
  <files>docs/sessions/mcp.md, docs/commands.md, README.md, CHANGELOG.md</files>
  <read_first>
    - docs/commands.md (heading style — `## relay <noun>` / `### relay <noun> <verb>`; mirror it for the mcp section)
    - README.md (current control-layer positioning — the MCP server is the "second front door" onto the same store/broker; match the existing voice, no marketing inflation)
    - CHANGELOG.md (entry format + the current unreleased/next-version heading to append under)
    - .planning/phases/09-mcp-server/09-04-SUMMARY.md + 09-05-SUMMARY.md (the confirmed tool-name set, the O-02 session-identity decision, and the SDK package name to cite accurately)
    - .planning/phases/09-mcp-server/09-CONTEXT.md (D-11 .mcp.json registration; D-07/D-08/D-09 security posture to summarize for users; O-01/O-03 scope notes)
  </read_first>
  <action>
    Create docs/sessions/mcp.md covering: what `relay mcp` is (a stdio MCP server, a second front door onto the same memory store + control broker, CLI unchanged); the exact `.mcp.json` entry for Claude Code and Claude Desktop (command `relay`, args `["mcp"]`, plus any env like RELAY_MEMORY_ALLOWED_WORKDIRS the user should scope); the eight tools grouped as memory (relay_memory_recall, relay_memory_save) and session-control (relay_session_list/inspect/send, relay_control_request_grant, relay_inbox_read/ack) with one-line descriptions; the security posture in plain terms — an MCP caller is an llm-kind control session, cross-session sends are default-deny and need a human grant, secrets are redacted at the boundary, the server logs only to stderr; and the v1 scope notes (O-01 dispatch deferred, O-03 save uses the same workdir scoping as the CLI). Add a `relay mcp` section to docs/commands.md listing the command + the eight tool names. Add a README mention under the control-layer positioning pointing to docs/sessions/mcp.md (no banned-word inflation — match the project's plain voice). Add a CHANGELOG entry under the appropriate version heading: "Relay can run as a stdio MCP server (`relay mcp`) exposing memory + session-control tools to MCP clients; additive, CLI unchanged." Cite the SDK package name accurately from the Plan 01/05 summaries. Do NOT invent tool names or behaviors not in the summaries.
  </action>
  <verify>
    <automated>test -f docs/sessions/mcp.md && grep -q "relay mcp" docs/commands.md && grep -q "relay_memory_recall" docs/sessions/mcp.md && grep -q "relay_session_send" docs/sessions/mcp.md && grep -qi "mcp" README.md && grep -qi "mcp" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/sessions/mcp.md exists with the `.mcp.json` block, all eight tool names, the security posture, and the O-01/O-03 scope notes.
    - docs/commands.md has a `relay mcp` section; README + CHANGELOG mention the capability.
    - Tool names + SDK package name match the Plan 04/05 summaries exactly (no invented names).
    - Prose follows the project voice (no banned words per global writing rules).
  </acceptance_criteria>
  <done>A user can mount Relay as an MCP server from the docs, knows the eight tools and the security posture, and the README/CHANGELOG reflect the shipped capability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MCP-07 final gate — live round-trip + suite green + CLI unchanged</name>
  <read_first>
    - docs/sessions/mcp.md (the .mcp.json block the human will paste into a scratch client config)
    - .planning/phases/09-mcp-server/09-05-SUMMARY.md (the registered tool-name set to expect when the client enumerates)
  </read_first>
  <action>The executor runs the automated gates below and reports their output, then hands off to the maintainer for the live MCP round-trip. The executor MUST NOT self-approve this gate — it pauses for the human after presenting the automated results and the .mcp.json block. Capture the maintainer's verdict and the live-verification details into the summary.</action>
  <what-built>
    The complete Phase 9 MCP server: `relay mcp` starts a stdio server exposing relay_memory_recall, relay_memory_save, and the six session-control tools, bound to a per-connection llm-kind control session under the Phase 8 broker, built from the verified SDK surface, with boundary redaction and strict stdout discipline. Docs + .mcp.json registration shipped.
  </what-built>
  <how-to-verify>
    1. Confirm automated gates first (the executor runs these and reports output):
       - `npm run build && npm test` → full suite green (no regressions vs the ~1861-test baseline; only the phase's new tests added).
       - `git diff <phase-base>..HEAD -- src/cli.ts` → additive only (one `mcp` branch + one help line; no existing branch changed).
       - `node dist/cli.js --help` → contains `mcp`, retains all prior MEMORY/SESSION/DELEGATION sections.
    2. Live MCP round-trip (human): add Relay to a scratch `.mcp.json` per docs/sessions/mcp.md and point Claude Code (or Claude Desktop, or the MCP Inspector) at it. Confirm:
       a. the client lists Relay's eight tools (enumeration works — MCP-01);
       b. calling relay_memory_save then relay_memory_recall round-trips a memory through the same store the CLI uses (run `relay memory recall` from the CLI and see the saved entry — proves MCP-02 shared store);
       c. calling relay_session_send to a registered target WITHOUT a grant returns a default-deny error result (proves MCP-04 broker parity over the live wire);
       d. nothing garbles the client connection (stdout discipline holding — D-11).
    3. Spot-check the stderr log shows diagnostics while stdout carried only protocol framing.
  </how-to-verify>
  <acceptance_criteria>
    - Automated: full suite green; `git diff` on src/cli.ts additive-only; `relay --help` additive-only.
    - Live: an MCP client enumerates the eight tools; a save→recall round-trips through the shared store; an ungranted send is default-denied over MCP.
    - No stdout corruption observed during the session.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live client enumerated the tools, the memory round-trip worked, and the ungranted send was denied — or describe the failure (which tool / which gate).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → user's client config | An inaccurate `.mcp.json` recipe could lead a user to mis-scope RELAY_MEMORY_ALLOWED_WORKDIRS and over-expose memory across projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-17 | Information Disclosure | docs under-specifying workdir scoping in .mcp.json | mitigate | docs/sessions/mcp.md explicitly shows scoping env and explains the security posture (Task 1); the live gate (Task 2) confirms scoping holds over the wire. |
| T-09-18 | Tampering | regression/CLI drift slipping in unnoticed | mitigate | The MCP-07 final gate (Task 2) runs the full suite + git-diff additive-only check before sign-off. |
</threat_model>

<verification>
- Automated gate in Task 2: `npm run build && npm test` green; src/cli.ts diff additive-only; `relay --help` additive-only.
- Docs grep gate in Task 1 confirms all eight tool names + the .mcp.json block are present.
- Human gate confirms the live enumeration + memory round-trip + default-deny send.
</verification>

<success_criteria>
- Relay-as-MCP-server is documented with a working `.mcp.json` recipe and the eight tools.
- The MCP-07 final gate passes: suite green, CLI additive-only, live round-trip verified.
</success_criteria>

<output>
Create `.planning/phases/09-mcp-server/09-06-SUMMARY.md` when done. Record the live-verification result (which client was used, the enumerated tool count, the round-trip + default-deny outcomes), the final test count vs baseline, and confirmation that src/cli.ts is additive-only. Note any open follow-ups (O-01 dispatch, O-03 stricter save consent) the maintainer deferred.
</output>

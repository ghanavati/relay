---
phase: 09-mcp-server
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - src/mcp/server.integration.test.ts
  - docs/sessions/mcp.md
  - docs/commands.md
  - README.md
  - CHANGELOG.md
  - SECURITY.md
autonomous: false
requirements: [MCP-01, MCP-02, MCP-05]
user_setup: []

must_haves:
  truths:
    - "An automated test drives the REAL SDK client against the REAL server (in-memory linked transport or child-process stdio) — enumeration and a memory round-trip pass over the actual protocol"
    - "A user can find the .mcp.json registration entry for Claude Code / Claude Desktop and mount Relay as an MCP server from the docs"
    - "docs/commands.md documents relay mcp and the two exposed tools; README + CHANGELOG reflect the shipped capability in the project's plain voice"
    - "SECURITY.md covers the MCP boundary: stdio trust model, workdir scoping, boundary redaction, stdout discipline, and the structural absence of dispatch/control over MCP"
    - "A human verifies end-to-end that a real MCP client enumerates Relay's tools and a memory recall round-trips; the full suite is green and the CLI surface unchanged — the MCP-05 final gate"
  artifacts:
    - path: "src/mcp/server.integration.test.ts"
      provides: "Real-SDK client↔server tests: tools/list enumeration, save→recall round-trip, workdir-forbidden isError over the wire"
      min_lines: 60
    - path: "docs/sessions/mcp.md"
      provides: "The relay mcp reference: what it exposes, the .mcp.json entry, security posture, v1 scope notes (memory-only surface; dispatch/control killed; remote transport = v2)"
      min_lines: 40
    - path: "docs/commands.md"
      provides: "A relay mcp command section + the two tool names"
      contains: "relay mcp"
  key_links:
    - from: "src/mcp/server.integration.test.ts"
      to: "src/mcp/server.ts"
      via: "startMcpServer driven by a real SDK Client over a linked transport"
      pattern: "startMcpServer"
    - from: "docs/sessions/mcp.md"
      to: ".mcp.json (user's client config)"
      via: "copy-paste registration block: command relay, args [mcp]"
      pattern: "mcp"
---

<objective>
Prove the phase end-to-end and document it. First an automated integration test that exercises the real MCP protocol — real SDK server, real SDK client — so wiring/framing/SDK regressions are caught in CI forever. Then the docs: the `.mcp.json` registration recipe, `relay mcp` reference, README/CHANGELOG/SECURITY updates. Close with the MCP-05 final gate: a human round-trips a tool call from a real MCP client, the full suite stays green, and the CLI is unchanged.

Purpose: MCP-01's success criterion is that a client registers Relay via `.mcp.json` and enumerates its tools — a documentation + verification deliverable, not just code. The automated integration test exists because of a hard lesson: Phase 8's TUI shipped 1804 green unit tests and was non-functional interactively, because no test drove the real interactive surface. This test closes that class for MCP — it drives the real SDK client against the real server, so "green units, dead live surface" cannot recur here. <!-- audit-added (PAUL) — finding preserved through the v0.4 rescope -->

Output: `src/mcp/server.integration.test.ts`, `docs/sessions/mcp.md`, updated docs/commands.md, README.md, CHANGELOG.md, SECURITY.md, plus the human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-mcp-server/09-CONTEXT.md
@.planning/RELAY-V04-SCOPE.md
@docs/commands.md
@README.md
@CHANGELOG.md
@SECURITY.md
@.planning/phases/09-mcp-server/09-02-SUMMARY.md
@.planning/phases/09-mcp-server/09-03-SUMMARY.md
@.planning/phases/09-mcp-server/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Automated MCP integration test — real SDK client ↔ server (MCP-01, MCP-02)</name>
  <files>src/mcp/server.integration.test.ts</files>
  <read_first>
    - 09-02-SUMMARY.md (resolveMcpSdk + whether the SDK ships a Client and an in-memory/linked transport pair, e.g. InMemoryTransport.createLinkedPair() — confirm the exact client + transport exports from the INSTALLED package before writing the test)
    - src/mcp/server.ts (startMcpServer with injected SDK/transport deps — from Plan 04)
    - src/control/tools.test.ts (in-memory store idiom: process.env['RELAY_DB_PATH']=':memory:' before imports)
  </read_first>
  <behavior>
    - Test 1 (enumeration, MCP-01): start the real McpServer via startMcpServer connected to one end of an in-memory linked transport; an SDK Client on the other end calls tools/list and gets exactly {relay_memory_recall, relay_memory_save}. This proves SDK wiring + registration actually speak MCP — not a mock of the server.
    - Test 2 (memory round-trip, MCP-02): the client calls relay_memory_save then relay_memory_recall over the wire and the saved memory comes back — hitting the same in-memory store.
    - Test 3 (scoping over the wire, MCP-02): with RELAY_MEMORY_ALLOWED_WORKDIRS excluding the requested workdir, a recall via the real client returns an isError result carrying MEMORY_WORKDIR_FORBIDDEN — proving the gate holds across the actual protocol boundary, not just in unit wrappers.
  </behavior>
  <action>
    Add src/mcp/server.integration.test.ts. Use the INSTALLED SDK's Client and an in-memory linked transport pair (confirm the exact export from the package via the Plan 02 summary / node_modules exports map — do not assume the symbol name). Connect startMcpServer's McpServer to the server end and an SDK Client to the client end; drive tools/list and tools/call through the real protocol objects. If the installed SDK does not export a usable in-process client+transport pair, fall back to spawning `node dist/cli.js mcp` as a child_process and speaking framed JSON-RPC over its stdio (initialize → tools/list → tools/call), asserting stdout carries only protocol framing — and record which approach was used in the summary. better-sqlite3 sync — no await on store calls.
  </action>
  <verify>
    <automated>npm run build && node --test dist/mcp/server.integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tools/list over a real SDK client returns exactly the two tool names (Test 1).
    - save→recall round-trips over the wire (Test 2); forbidden workdir is isError over the wire (Test 3).
    - The test drives the real SDK server/client (or a real relay mcp subprocess), not a mock of either.
  </acceptance_criteria>
  <done>An automated test exercises the real MCP protocol end-to-end, closing the "green unit tests, dead live surface" gap that bit the TUI.</done>
</task>

<task type="auto">
  <name>Task 2: Docs — .mcp.json registration, command reference, README/CHANGELOG/SECURITY (MCP-01)</name>
  <files>docs/sessions/mcp.md, docs/commands.md, README.md, CHANGELOG.md, SECURITY.md</files>
  <read_first>
    - docs/commands.md (heading style — mirror it for the mcp section)
    - README.md (current positioning — Relay is a delegation engine + cross-tool memory; the MCP server is the second front door onto the same store; match the existing voice, no marketing inflation)
    - CHANGELOG.md (entry format + the current unreleased/next-version heading)
    - SECURITY.md (existing threat-model tone to extend)
    - 09-04-SUMMARY.md (the confirmed tool-name set + SDK package name to cite accurately)
  </read_first>
  <action>
    Create docs/sessions/mcp.md covering: what `relay mcp` is (a stdio MCP server exposing Relay's cross-session, cross-tool memory to any MCP client; CLI unchanged); the exact `.mcp.json` entry for Claude Code and Claude Desktop (command `relay`, args `["mcp"]`, plus RELAY_MEMORY_ALLOWED_WORKDIRS scoping env the user should set); the two tools with one-line descriptions; reach notes (works in any MCP-client app on stdio — Claude Desktop/Code, Cursor, Codex, Windsurf, and harnesses that run those agents; ChatGPT/web clients need the v2 remote transport + OAuth, deferred); the security posture in plain terms — workdir scoping, secrets redacted at the boundary, server logs only to stderr, stdio trusts the OS user, and no dispatch/shell/session-control surface exists over MCP. Add a `relay mcp` section to docs/commands.md listing the command + the two tool names. Add a README mention pointing to docs/sessions/mcp.md (plain voice, no banned words). Add a CHANGELOG entry: Relay runs as a stdio MCP server (`relay mcp`) exposing memory recall/save to MCP clients; additive, CLI unchanged. Update SECURITY.md with an "MCP server (relay mcp)" subsection: stdio inherits the OS-user trust boundary (a local process that can pipe into relay mcp stdin acts as the user); memory workdir scoping applies; boundary redaction on every returned value; stdout protocol discipline; dispatch/control structurally absent over MCP; remote transports + auth are explicitly out of scope until v2. Cite the SDK package name accurately from the summaries. Do NOT invent tool names or behaviors not in the summaries.
  </action>
  <verify>
    <automated>test -f docs/sessions/mcp.md && grep -q "relay mcp" docs/commands.md && grep -q "relay_memory_recall" docs/sessions/mcp.md && grep -q "relay_memory_save" docs/sessions/mcp.md && grep -qi "mcp" README.md && grep -qi "mcp" CHANGELOG.md && grep -qi "mcp" SECURITY.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/sessions/mcp.md exists with the .mcp.json block, both tool names, the security posture, and the v1 scope/reach notes.
    - docs/commands.md has a relay mcp section; README + CHANGELOG + SECURITY mention the capability.
    - Tool names + SDK package name match the summaries exactly (no invented names).
    - Prose follows the project voice (no banned words per global writing rules).
  </acceptance_criteria>
  <done>A user can mount Relay as an MCP server from the docs, knows the two tools and the security posture, and README/CHANGELOG/SECURITY reflect the shipped capability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: MCP-05 final gate — live round-trip + suite green + CLI unchanged</name>
  <read_first>
    - docs/sessions/mcp.md (the .mcp.json block the human will paste into a scratch client config)
    - 09-04-SUMMARY.md (the registered tool-name set to expect when the client enumerates)
  </read_first>
  <action>The executor runs the automated gates below and reports their output, then hands off to the maintainer for the live MCP round-trip. The executor MUST NOT self-approve this gate — it pauses for the human after presenting the automated results and the .mcp.json block. Capture the maintainer's verdict and the live-verification details into the summary.</action>
  <what-built>
    The complete Phase 9 v0.4 slice: agnostic provider dispatch (env-declared providers through the registry) and `relay mcp` — a stdio server exposing relay_memory_recall + relay_memory_save, built from the verified SDK surface, with boundary redaction and strict stdout discipline. Docs + .mcp.json registration shipped.
  </what-built>
  <how-to-verify>
    1. Confirm automated gates first (the executor runs these and reports output):
       - `npm run build && npm test` → full suite green (no regressions vs the ~1804-test baseline; only the phase's new tests added).
       - `git diff <phase-base>..HEAD -- src/cli.ts` → additive only (mcp + providers branches and help lines; no existing branch changed).
       - `node dist/cli.js --help` → contains mcp + providers, retains all prior sections.
       - `node dist/cli.js providers` → lists builtins; with a synthetic RELAY_PROVIDER_DEMO_URL set, lists demo with no key value printed.
    2. Live MCP round-trip (human): add Relay to a scratch `.mcp.json` per docs/sessions/mcp.md and point Claude Code (or Claude Desktop, or the MCP Inspector) at it. Confirm:
       a. the client lists Relay's two tools (enumeration works — MCP-01);
       b. calling relay_memory_save then relay_memory_recall round-trips a memory through the same store the CLI uses (run `relay memory recall` from the CLI and see the saved entry — proves MCP-02 shared store);
       c. nothing garbles the client connection (stdout discipline holding).
    3. Spot-check the stderr log shows diagnostics while stdout carried only protocol framing.
  </how-to-verify>
  <acceptance_criteria>
    - Automated: full suite green; src/cli.ts diff additive-only; help additive-only; providers listing key-safe.
    - Live: an MCP client enumerates the two tools; a save→recall round-trips through the shared store.
    - No stdout corruption observed during the session.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the live client enumerated the tools and the memory round-trip worked — or describe the failure (which tool / which gate).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → user's client config | An inaccurate .mcp.json recipe could lead a user to mis-scope RELAY_MEMORY_ALLOWED_WORKDIRS and over-expose memory across projects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | Information Disclosure | docs under-specifying workdir scoping in .mcp.json | mitigate | docs/sessions/mcp.md explicitly shows the scoping env and explains the posture (Task 2); the integration test (Task 1, Test 3) proves scoping holds over the wire. |
| T-09-16 | Tampering | regression/CLI drift slipping in unnoticed | mitigate | The final gate (Task 3) runs the full suite + git-diff additive-only check before sign-off. |
</threat_model>

<verification>
- Automated gates in Task 3: `npm run build && npm test` green; src/cli.ts diff additive-only; help additive-only.
- Docs grep gate in Task 2 confirms both tool names + the .mcp.json block.
- Human gate confirms live enumeration + memory round-trip.
</verification>

<success_criteria>
- The real-protocol integration test passes in CI; Relay-as-MCP-server is documented with a working .mcp.json recipe and the two tools; the MCP-05 final gate passes: suite green, CLI additive-only, live round-trip verified.
</success_criteria>

<output>
Create `.planning/phases/09-mcp-server/09-05-SUMMARY.md` when done. Record the live-verification result (which client, enumerated tool count, round-trip outcome), the integration-test approach used (in-memory pair vs subprocess), the final test count vs baseline, and confirmation that src/cli.ts is additive-only.
</output>

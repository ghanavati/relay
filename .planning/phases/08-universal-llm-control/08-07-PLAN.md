---
phase: 08-universal-llm-control
plan: 07
type: tdd
wave: 5
depends_on:
  - 08-03-PLAN
  - 08-06-PLAN
files_modified:
  - src/cli/cmd-tui.ts
  - src/cli/cmd-tui.test.ts
  - src/cli/cmd-session.ts
  - src/control/broker.ts
  - src/control/broker.test.ts
  - src/control/session-store.ts
autonomous: true
requirements:
  - CONTROL-13
  - CONTROL-14
  - CONTROL-15
must_haves:
  truths:
    - "D-13: Human UI actions and LLM tool actions use the same broker methods, policy checks, grants, loop detection, and audit events."
    - "D-14: Model-driven Command Central is broker-mediated and visible; models cannot approve their own grants or silently raise authority."
    - "CONTROL-13/14/15: TUI actions and approval queues route through broker policy and prevent self-escalation."
---

<objective>
Add Command Central command actions, inbox, and grant approval queue through the broker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-universal-llm-control/EXECUTOR-COMPATIBILITY.md
@.planning/phases/08-universal-llm-control/CONTEXT.md
@.planning/phases/08-universal-llm-control/08-03-PLAN.md
@.planning/phases/08-universal-llm-control/08-06-PLAN.md
@src/control/broker.ts
@src/cli/cmd-session.ts
@src/cli/cmd-tui.ts
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Add command palette actions through the broker</name>
  <files>src/cli/cmd-tui.ts, src/cli/cmd-session.ts, src/control/broker.ts, src/cli/cmd-tui.test.ts</files>
  <action>RED: test palette action handlers for send, inspect, tail, grant, revoke, delegate, pause, and resume using fake sessions, including unsupported capability failures. GREEN: implement keyboard-first command palette actions that call shared broker/session-command functions directly, not shelling out and not mutating UI state as source of truth. This implements D-13 and CONTROL-13.</action>
  <verify>Run `npm run build && node --test dist/cli/cmd-tui.test.js dist/cli/cmd-session.test.js`.</verify>
  <done>Human actions in Command Central produce the same broker events and errors as `relay session ...` CLI actions.</done>
</task>

<task type="tdd">
  <name>Task 2: Add inbox and grant approval queue</name>
  <files>src/cli/cmd-tui.ts, src/control/broker.ts, src/control/session-store.ts, src/cli/cmd-tui.test.ts, src/control/broker.test.ts</files>
  <action>RED: test pending LLM requests, human approve, human deny, expired pending request, and model self-approval denial. GREEN: surface pending cross-session requests in Command Central and route approve/deny through broker grant APIs with TTL, message budget, source, target, and audit event creation. This implements D-14, CONTROL-14, and CONTROL-15.</action>
  <verify>Run `npm run build && node --test dist/cli/cmd-tui.test.js dist/control/broker.test.js`.</verify>
  <done>Command Central shows what models are asking to do and lets the human approve or deny without giving models self-escalation.</done>
</task>
</tasks>

<verification>
Run `npm run build && node --test dist/cli/cmd-tui.test.js dist/cli/cmd-session.test.js dist/control/broker.test.js`.
</verification>

<success_criteria>
1. TUI commands route through the same broker as CLI/tools.
2. Pending model requests are visible and approvable/deniable.
3. Models cannot approve their own grants or silently raise authority.
</success_criteria>

<output>
Create `.planning/phases/08-universal-llm-control/08-07-SUMMARY.md` when done.
</output>

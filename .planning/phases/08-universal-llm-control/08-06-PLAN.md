---
phase: 08-universal-llm-control
plan: 06
type: tdd
wave: 4
depends_on:
  - 08-02-PLAN
files_modified:
  - src/control/read-model.ts
  - src/control/read-model.test.ts
  - src/control/session-store.ts
  - src/cli/cmd-tui.ts
  - src/cli/cmd-tui.test.ts
autonomous: true
requirements:
  - CONTROL-11
  - CONTROL-12
  - CONTROL-17
must_haves:
  truths:
    - "D-11: Command Central is terminal-native Ink over the control broker, not a browser dashboard or second control implementation."
    - "D-12: The TUI consumes a shared ControlSnapshot read model also used by relay tui --json; UI code does not add independent SQL paths."
    - "D-15: Command Central is Herdr-inspired: compact terminal panes, workspace/session rail, state rollups, keyboard-first command palette, and operational controls."
    - "CONTROL-11/12/17: Command Central has a shared read model and terminal-native operator layout."
---

<objective>
Turn the current TUI snapshot into a broker-backed Command Central read model and terminal layout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-universal-llm-control/EXECUTOR-COMPATIBILITY.md
@.planning/phases/08-universal-llm-control/CONTEXT.md
@src/cli/cmd-tui.ts
@src/cli/cmd-tui.test.ts
@src/control/types.ts
@src/control/session-store.ts
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Add broker-backed ControlSnapshot read model</name>
  <files>src/control/read-model.ts, src/control/read-model.test.ts, src/control/session-store.ts</files>
  <action>RED: test gatherControlSnapshot against an in-memory store with multiple sessions, bounded events, queued inbox items, grants, blocked events, and provider status summaries. GREEN: implement immutable ControlSnapshot and summary types that read through ControlSessionStore/broker helpers with explicit limits and no TUI-owned SQL. This implements D-12 and CONTROL-11.</action>
  <verify>Run `npm run build && node --test dist/control/read-model.test.js`.</verify>
  <done>ControlSnapshot contains session roster, selected session, event tail, inbox, grants, pending actions, audit items, and status summaries with bounded reads.</done>
</task>

<task type="tdd">
  <name>Task 2: Upgrade relay tui --json to emit Command Central snapshots</name>
  <files>src/cli/cmd-tui.ts, src/cli/cmd-tui.test.ts, src/control/read-model.ts</files>
  <action>RED: update JSON-mode tests to assert the snapshot includes sessions, events, inbox, grants, pending_actions, audit, and legacy health fields needed by scripts. GREEN: route gatherSnapshot through gatherControlSnapshot while preserving `relay tui --json` as a single bounded snapshot for automation. This implements D-12 and CONTROL-11.</action>
  <verify>Run `npm run build && node --test dist/cli/cmd-tui.test.js dist/control/read-model.test.js`.</verify>
  <done>`relay tui --json` is the machine-readable Command Central state contract.</done>
</task>

<task type="tdd">
  <name>Task 3: Build Herdr-inspired terminal Command Central layout</name>
  <files>src/cli/cmd-tui.ts, src/cli/cmd-tui.test.ts</files>
  <action>RED: add render-shape tests around empty, active, blocked, and narrow terminal snapshots without depending on provider network calls. GREEN: replace the three-panel dashboard with compact Ink panes: left session/workspace rail, main selected session/event pane, right inbox/grants queue, bottom audit/status/cost strip, and command hints. Use state/capability badges instead of verbose explanatory text. This implements D-11, D-15, CONTROL-12, and CONTROL-17.</action>
  <verify>Run `npm run build && node --test dist/cli/cmd-tui.test.js`.</verify>
  <done>`relay tui` opens directly into Command Central and makes active, blocked, done, and idle sessions scannable without opening every transcript.</done>
</task>
</tasks>

<verification>
Run `npm run build && node --test dist/control/read-model.test.js dist/cli/cmd-tui.test.js`.
</verification>

<success_criteria>
1. Command Central uses a shared read model, not UI-owned SQL.
2. `relay tui --json` exposes the snapshot contract.
3. `relay tui` renders a terminal-native operator console shape.
</success_criteria>

<output>
Create `.planning/phases/08-universal-llm-control/08-06-SUMMARY.md` when done.
</output>

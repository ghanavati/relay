---
phase: 08-universal-llm-control
plan: 08
type: tdd
wave: 6
depends_on:
  - 08-07-PLAN
files_modified:
  - src/control/tools.ts
  - src/control/tools.test.ts
  - src/control/broker.ts
  - src/control/read-model.ts
  - src/control/read-model.test.ts
  - src/cli/cmd-tui.ts
  - src/cli/cmd-tui.test.ts
autonomous: true
requirements:
  - CONTROL-14
  - CONTROL-15
  - CONTROL-16
must_haves:
  truths:
    - "D-14: Model-driven Command Central is broker-mediated and visible."
    - "D-15: Command Central is operational and fast, not a passive marketing dashboard."
    - "CONTROL-14/15/16: Model operations are visible, non-self-escalating, and the TUI remains responsive under load."
---

<objective>
Make model-driven Command Central operations visible and keep the TUI responsive under active sessions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-universal-llm-control/EXECUTOR-COMPATIBILITY.md
@.planning/phases/08-universal-llm-control/CONTEXT.md
@.planning/phases/08-universal-llm-control/08-07-PLAN.md
@src/control/tools.ts
@src/control/read-model.ts
@src/cli/cmd-tui.ts
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Show model-driven operations as first-class UI events</name>
  <files>src/control/tools.ts, src/control/broker.ts, src/control/read-model.ts, src/cli/cmd-tui.ts, src/control/tools.test.ts, src/control/read-model.test.ts</files>
  <action>RED: test that relay_session_send, relay_control_request_grant, inbox read, and inbox ack create readable audit/event entries with caller session, requested action, decision, and denial reason. GREEN: add source badges and pending/executed/denied states to the read model and TUI. This implements D-13, D-14, CONTROL-14, and CONTROL-15.</action>
  <verify>Run `npm run build && node --test dist/control/tools.test.js dist/control/read-model.test.js dist/cli/cmd-tui.test.js`.</verify>
  <done>The UI makes model control visible instead of hiding it behind tool-call logs.</done>
</task>

<task type="tdd">
  <name>Task 2: Keep Command Central fast under load</name>
  <files>src/control/read-model.ts, src/cli/cmd-tui.ts, src/control/read-model.test.ts, src/cli/cmd-tui.test.ts</files>
  <action>RED: add tests for bounded event limits, cancellation of stale refreshes, provider probe timeout behavior, and large session sets. GREEN: enforce limit defaults, avoid unbounded awaits on the Ink render path, cancel or ignore stale refresh promises, and degrade provider status to offline or unknown without blocking the UI. This implements CONTROL-16.</action>
  <verify>Run `npm run build && node --test dist/control/read-model.test.js dist/cli/cmd-tui.test.js`.</verify>
  <done>Command Central remains responsive with many sessions, offline providers, and frequent event writes.</done>
</task>
</tasks>

<verification>
Run `npm run build && node --test dist/control/tools.test.js dist/control/read-model.test.js dist/cli/cmd-tui.test.js`.
</verification>

<success_criteria>
1. Model-driven operations are first-class audit/read-model/TUI entries.
2. TUI refreshes are bounded and cancellable.
3. Offline providers do not block Command Central.
</success_criteria>

<output>
Create `.planning/phases/08-universal-llm-control/08-08-SUMMARY.md` when done.
</output>

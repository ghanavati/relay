---
phase: 08-universal-llm-control
plan: 02
type: tdd
wave: 2
depends_on:
  - 08-01-PLAN
files_modified:
  - src/control/broker.ts
  - src/control/broker.test.ts
  - src/control/adapter-registry.ts
  - src/control/adapters/fake.ts
  - src/control/adapter-registry.test.ts
  - src/errors.ts
autonomous: true
requirements:
  - CONTROL-03
  - CONTROL-04
  - CONTROL-10
must_haves:
  truths:
    - "D-03: LLM-to-LLM control flows through Relay tools and brokered messages, never direct terminal writes."
    - "D-04: LLM-initiated cross-session sends are default-deny and require grants, TTLs, budgets, and loop detection."
    - "D-06: Secrets and private transcript content are redacted before crossing session boundaries."
    - "CONTROL-03/04/10: Sends route through policy, grants, redaction, budgets, and loop detection."
---

<objective>
Build the policy-aware control broker and deterministic fake adapter harness.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-universal-llm-control/EXECUTOR-COMPATIBILITY.md
@.planning/phases/08-universal-llm-control/CONTEXT.md
@.planning/phases/08-universal-llm-control/08-01-PLAN.md
@src/control/types.ts
@src/control/session-store.ts
@src/errors.ts
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Build policy-aware broker with grants and loop detection</name>
  <files>src/control/broker.ts, src/control/broker.test.ts, src/errors.ts</files>
  <action>RED: test human sends, LLM sends without grants, expired grants, exhausted budgets, self-send blocking, repeated-content loop detection, and redaction before persistence. GREEN: implement ControlBroker with sendMessage, checkGrant, normalized content hashing, TTL checks, budget decrement in one transaction, and event append for queued, blocked, delivered, and failed. Use RelayError for user-facing failures. This implements D-03, D-04, D-05, D-06, D-10, CONTROL-03, CONTROL-04, CONTROL-10.</action>
  <verify>Run `npm run build && node --test dist/control/broker.test.js`.</verify>
  <done>LLM-to-LLM sends are denied by default, granted sends are auditable, and ping-pong loops are blocked before unbounded growth.</done>
</task>

<task type="tdd">
  <name>Task 2: Add adapter registry and fake adapter harness</name>
  <files>src/control/adapter-registry.ts, src/control/adapters/fake.ts, src/control/adapter-registry.test.ts</files>
  <action>RED: test duplicate adapter rejection, capability-based send routing, unsupported-operation RelayError output, and fake A-to-B/B-to-A delivery. GREEN: implement adapter registry plus deterministic fake adapter with no real filesystem access. This proves the universal surface before provider-specific behavior. This implements D-01, D-03, CONTROL-01, CONTROL-03.</action>
  <verify>Run `npm run build && node --test dist/control/adapter-registry.test.js`.</verify>
  <done>Fake sessions exchange messages bidirectionally through the same broker and registry path used by real adapters.</done>
</task>
</tasks>

<verification>
Run `npm run build && node --test dist/control/broker.test.js dist/control/adapter-registry.test.js`.
</verification>

<success_criteria>
1. Cross-session messages flow through broker policy only.
2. LLM sends require grants and are loop/budget bounded.
3. Fake adapters prove bidirectional delivery without provider dependencies.
</success_criteria>

<output>
Create `.planning/phases/08-universal-llm-control/08-02-SUMMARY.md` when done.
</output>

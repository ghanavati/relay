---
phase: 08-universal-llm-control
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/control/types.ts
  - src/control/types.test.ts
  - src/control/session-store.ts
  - src/control/session-store.test.ts
  - src/runtime/store/db.ts
autonomous: true
requirements:
  - CONTROL-01
  - CONTROL-02
must_haves:
  truths:
    - "D-01: Relay models delivery and control as explicit adapter capabilities, not provider-name assumptions."
    - "D-02: Strong live control exists only for provider APIs or Relay-owned processes; ambient sessions use truthful mailbox/context delivery."
    - "D-05: Every control event is auditable with source, target, status, delivery attempts, content hash, and redaction metadata."
    - "CONTROL-01/02: Session registry, capability reporting, inspection, and tailable events exist before adapters or UI are built."
---

<objective>
Create the universal control foundation: typed capability taxonomy and synchronous SQLite session-control store.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-universal-llm-control/EXECUTOR-COMPATIBILITY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-universal-llm-control/CONTEXT.md
@src/runtime/store/db.ts
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Define universal control types and capability taxonomy</name>
  <files>src/control/types.ts, src/control/types.test.ts</files>
  <action>RED: test the closed ControlCapability set, provider/session state values, Zod rejection of malformed sessions, and Zod rejection of LLM sends missing source, target, or content. GREEN: create immutable ControlCapability, ControlSession, ControlEvent, ControlMessage, ControlGrant, DeliveryAttempt, Zod schemas, and ControlAdapter. This implements D-01, D-02, D-05, CONTROL-01.</action>
  <verify>Run `npm run build && node --test dist/control/types.test.js`.</verify>
  <done>Types compile, schemas reject malformed boundary input, and capabilities are explicit rather than inferred from provider names.</done>
</task>

<task type="tdd">
  <name>Task 2: Add synchronous SQLite control store</name>
  <files>src/runtime/store/db.ts, src/control/session-store.ts, src/control/session-store.test.ts</files>
  <action>RED: test creation and use of `control_sessions`, `control_events`, `control_mailbox`, `control_grants`, and `control_delivery_attempts` using `RELAY_DB_PATH=':memory:'` before imports. GREEN: implement ControlSessionStore with sync better-sqlite3 transactions for upsertSession, getSession, listSessions, appendEvent, tailEvents, enqueueMessage, getQueuedMessages, delivery status transitions, grant, revoke, and getGrant. Do not await DB calls. This implements D-02, D-05, CONTROL-02.</action>
  <verify>Run `npm run build && node --test dist/control/session-store.test.js`.</verify>
  <done>Store operations preserve immutability, append ordered events, status every message, and reject malformed capabilities JSON.</done>
</task>
</tasks>

<verification>
Run `npm run build && node --test dist/control/types.test.js dist/control/session-store.test.js`.
</verification>

<success_criteria>
1. Control capabilities and session/message/grant/event schemas are typed and boundary-validated.
2. SQLite session-control tables support registry, event, mailbox, grant, and delivery-attempt flows.
3. No async/await is used for better-sqlite3 operations.
</success_criteria>

<output>
Create `.planning/phases/08-universal-llm-control/08-01-SUMMARY.md` when done.
</output>

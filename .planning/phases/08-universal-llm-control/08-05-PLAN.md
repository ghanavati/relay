---
phase: 08-universal-llm-control
plan: 05
type: tdd
wave: 4
depends_on:
  - 08-03-PLAN
  - 08-04-PLAN
files_modified:
  - src/control/pty-session.ts
  - src/control/pty-session.test.ts
  - src/cli/cmd-session.ts
  - src/control/control-e2e.test.ts
  - src/cli/cmd-verify.ts
  - src/cli/cmd-doctor.ts
  - src/cli/cmd-info.ts
  - docs/commands.md
  - docs/architecture.md
  - docs/providers.md
  - docs/configuration.md
  - README.md
  - CHANGELOG.md
autonomous: true
requirements:
  - CONTROL-01
  - CONTROL-02
  - CONTROL-03
  - CONTROL-10
must_haves:
  truths:
    - "D-02: Strong live control exists for Relay-owned processes."
    - "D-05: Diagnostics expose every control event, delivery state, and blocked decision."
    - "CONTROL-01/02/03/10: End-to-end control is verified across fake, LM Studio, Claude, and blocked-loop paths."
---

<objective>
Add Relay-owned process sessions, integrated verification, diagnostics, and baseline control docs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-universal-llm-control/EXECUTOR-COMPATIBILITY.md
@.planning/phases/08-universal-llm-control/CONTEXT.md
@.planning/phases/08-universal-llm-control/08-03-PLAN.md
@.planning/phases/08-universal-llm-control/08-04-PLAN.md
@src/cli/cmd-verify.ts
@src/cli/cmd-doctor.ts
@src/cli/cmd-info.ts
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Add Relay-owned CLI session wrapper</name>
  <files>src/control/pty-session.ts, src/control/pty-session.test.ts, src/cli/cmd-session.ts</files>
  <action>RED: test fake child process spawn, stdin send, stdout/stderr tail events, SIGINT interrupt, and stopped-state recording. GREEN: implement minimal Relay-owned process wrapper using node child_process first; add a PTY dependency only if tests prove interactive terminal semantics require it. v1 scope decision (2026-06-07): `live_stdin` covers line-based subprocess I/O via child_process pipes only. Full-TTY interactive CLIs (claude, codex) detect non-TTY stdio and change behavior, so they are out of `live_stdin` scope until node-pty is explicitly approved as a dependency — their adapters must report `live_stdin` as absent. This implements D-02 and CONTROL-03 for strong live_stdin sessions.</action>
  <verify>Run `npm run build && node --test dist/control/pty-session.test.js dist/cli/cmd-session.test.js`.</verify>
  <done>`relay session spawn --adapter <name> -- <command...>` creates a session Relay can observe, tail, send to, and interrupt.</done>
</task>

<task type="tdd">
  <name>Task 2: Add end-to-end verification and diagnostics</name>
  <files>src/control/control-e2e.test.ts, src/cli/cmd-verify.ts, src/cli/cmd-doctor.ts, src/cli/cmd-info.ts</files>
  <action>RED: write E2E tests for fake A-to-B reply, LM Studio control-tool send to fake target, Claude queued context rendering, unauthorized agent send blocking, and repeated ping-pong loop blocking. GREEN: add verify, doctor, and info checks for session tables, registered adapter capabilities, queued delivery count, and blocked control attempts. This implements CONTROL-01 through CONTROL-10 as integrated behavior.</action>
  <verify>Run `npm run build && node --test dist/control/control-e2e.test.js dist/cli/cmd-verify.test.js dist/cli/cmd-doctor.test.js dist/cli/cmd-info.test.js`.</verify>
  <done>Any-to-any messaging is proven through fake, LM Studio, and Claude paths, and diagnostics surface health and blocked attempts.</done>
</task>

<task type="tdd">
  <name>Task 3: Update control-layer docs and changelog</name>
  <files>README.md, docs/commands.md, docs/architecture.md, docs/providers.md, docs/configuration.md, CHANGELOG.md</files>
  <action>RED: add doc checks that README does not claim every adapter supports live control, commands docs cover every session subcommand, and provider docs include capability levels. GREEN: document the control fabric, adapter capability taxonomy, human-to-Claude, LM Studio-to-Claude, Claude-to-LM Studio, Codex-to-Claude, and Relay-owned CLI strong mode. This implements D-01 through D-10 as user-facing contract.</action>
  <verify>Run `npm run build && npm test`.</verify>
  <done>Docs describe universal command surface with truthful adapter-specific delivery semantics, and CHANGELOG has an Unreleased entry.</done>
</task>
</tasks>

<verification>
Run `npm run build && npm test && relay verify --json && relay doctor --json`.
</verification>

<success_criteria>
1. Relay-owned sessions support strong process control.
2. E2E tests prove allowed and denied cross-session paths.
3. Diagnostics and docs expose truthful capability semantics.
</success_criteria>

<output>
Create `.planning/phases/08-universal-llm-control/08-05-SUMMARY.md` when done.
</output>

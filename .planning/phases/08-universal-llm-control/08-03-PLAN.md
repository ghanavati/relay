---
phase: 08-universal-llm-control
plan: 03
type: tdd
wave: 3
depends_on:
  - 08-02-PLAN
files_modified:
  - src/cli/cmd-session.ts
  - src/cli/cmd-session.test.ts
  - src/cli.ts
  - src/control/tools.ts
  - src/control/tools.test.ts
  - src/workers/lmstudio-agentic.ts
  - src/cli/cmd-run.ts
  - docs/commands.md
autonomous: true
requirements:
  - CONTROL-01
  - CONTROL-02
  - CONTROL-03
  - CONTROL-04
  - CONTROL-05
  - CONTROL-07
must_haves:
  truths:
    - "D-03: Human CLI and LLM tools both use Relay brokered messages."
    - "D-04: LLM-facing tools enforce grants, TTLs, budgets, and loop detection."
    - "D-08: LM Studio agentic sessions are Relay-native strong-control sessions."
    - "CONTROL-05/07: Relay exposes LLM-facing control tools and wires them into LM Studio agentic sessions."
---

<objective>
Expose the control broker to humans through `relay session ...` and to models through Relay control tools.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-universal-llm-control/EXECUTOR-COMPATIBILITY.md
@.planning/phases/08-universal-llm-control/CONTEXT.md
@.planning/phases/08-universal-llm-control/08-02-PLAN.md
@src/cli.ts
@src/cli/cmd-run.ts
@src/workers/lmstudio-agentic.ts
@src/workers/types.ts
</context>

<tasks>
<task type="tdd">
  <name>Task 1: Expose human CLI through relay session commands</name>
  <files>src/cli/cmd-session.ts, src/cli/cmd-session.test.ts, src/cli.ts, docs/commands.md</files>
  <action>RED: test `relay session list`, `inspect`, `tail`, `send`, `grant`, and `revoke` in JSON mode plus unsupported target capability exit behavior. GREEN: add executeSessionCommand, parse a new `session` branch in `src/cli.ts`, validate CLI args with Zod, and route to store, broker, and registry. This implements CONTROL-01 through CONTROL-04.</action>
  <verify>Run `npm run build && node --test dist/cli/cmd-session.test.js`.</verify>
  <done>User can inspect and send to registered sessions from CLI, and unsupported adapter operations fail clearly.</done>
</task>

<task type="tdd">
  <name>Task 2: Add LLM-facing Relay control tools and LM Studio wiring</name>
  <files>src/control/tools.ts, src/control/tools.test.ts, src/workers/lmstudio-agentic.ts, src/cli/cmd-run.ts</files>
  <action>RED: test `relay_session_list`, `relay_session_inspect`, `relay_session_send`, `relay_inbox_read`, and `relay_inbox_ack` handlers, including caller session scoping and grant enforcement. GREEN: expose OpenAI-compatible ToolDef declarations and handlers, then wire them through LmStudioAgenticRunner's existing extraToolHandlers path and create a control session for every `relay run --provider lmstudio-agentic`. This implements D-04, D-05, D-08, CONTROL-05, CONTROL-07.</action>
  <verify>Run `npm run build && node --test dist/control/tools.test.js dist/workers/lmstudio-agentic.test.js`.</verify>
  <done>Relay-owned LM Studio sessions can send to and receive from other sessions through Relay tools without bypassing policy.</done>
</task>
</tasks>

<verification>
Run `npm run build && node --test dist/cli/cmd-session.test.js dist/control/tools.test.js dist/workers/lmstudio-agentic.test.js`.
</verification>

<success_criteria>
1. Human control and model control share the broker path.
2. LM Studio agentic sessions can participate bidirectionally through Relay tools.
3. CLI and JSON output expose adapter capabilities and clear failures.
</success_criteria>

<output>
Create `.planning/phases/08-universal-llm-control/08-03-SUMMARY.md` when done.
</output>

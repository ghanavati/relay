# Session 2026-05-03 20:19

**Session ID:** caa5d0cc-b24e-4e53-b19d-bc03fa559441

---

**User** `2026-05-03T18:23:27.199Z`

How to do this with LM studio: The /model picker now lists models from your gateway's /v1/models endpoint when ANTHROPIC_BASE_URL points at an Anthropic-compatible gateway

---

**Assistant** `2026-05-03T18:23:48.904Z`

I'll research the exact setup for pointing Claude Code at LM Studio via the new `/v1/models` discovery feature, since LM Studio speaks OpenAI's API format while Claude Code sends Anthropic-format requests — there's a translation layer needed.

---

**Assistant** `2026-05-03T18:25:15.110Z`

`★ Insight ─────────────────────────────────────`
- The new 2.1.126 feature treats `ANTHROPIC_BASE_URL` as a *generic* Anthropic-API-compatible host and probes `GET /v1/models` to populate the picker. So the gateway has to speak Anthropic format on `/v1/messages` AND expose a model list at `/v1/models`.
- LM Studio actually qualifies: since v0.4.1 it ships a native Anthropic-compat surface alongside its OpenAI server, and the OpenAI server's `/v1/models` lives at the same host — so one process answers both shapes.
- This is the cleanest "no proxy" path. The classic translator-proxy route (claude-code-router, anthropic-bridge, LiteLLM) is still useful when you want routing/fallback, but you don't *need* it just for LM Studio anymore.
`─────────────────────────────────────────────────`

## Setup

**1. Start the LM Studio server** (load a model with ≥32k context — Claude Code's prompts are large):

`[code block — 2 lines]`

**2. Point Claude Code at it:**

`[code block — 6 lines]`

**3. Verify the picker** — inside Claude Code run `/model`. You should see LM Studio's loaded models populated from `/v1/models` (instead of the hardcoded Anthropic list).

## Sanity check before launching `claude`

`[code block — 8 lines]`

If `/v1/messages` 404s, your LM Studio is older than 0.4.1 — upgrade, or fall back to a translator proxy (`claude-code-router` is the most maintained option).

## When to use a proxy instead

- You want to mix LM Studio with cloud models (Groq, OpenRouter, real Anthropic) behind one URL → use **claude-code-router**.
- You want different models for "main" vs "small/fast" routed by request size → use a router.
- You want logging/tracing → LiteLLM proxy gives you that.

Want me to set the env vars in a shell rc file or a per-project `.envrc` so this sticks across sessions?

---

**User** `2026-05-03T18:26:02.056Z`

How many tokens did you use in this startup of claude?

---

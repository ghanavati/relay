# Session 2026-05-03 20:42

**Session ID:** 6c31a119-6393-42f7-82f3-384912c53ea0

---

**User** `2026-05-03T18:42:18.164Z`

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

**User** `2026-05-03T18:42:18.164Z`

<command-name>/context</command-name>
            <command-message>context</command-message>
            <command-args></command-args>

---

**User** `2026-05-03T18:42:18.164Z`

<local-command-stdout> [1mContext Usage[22m
[38;2;136;136;136m⛁ ⛁ [38;2;153;153;153m⛁ ⛁ [38;2;177;185;249m⛁ [38;2;215;119;87m⛁ [38;2;255;193;7m⛁ ⛀ [38;2;147;51;234m⛀ [38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  Opus 4.7 (1M context)
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;153;153;153mclaude-opus-4-7[1m][39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;153;153;153m45.9k/1m tokens (5%)[39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;153;153;153m[3mEstimated usage by category[23m[39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;136;136;136m⛁[39m System prompt: [38;2;153;153;153m8.8k tokens (0.9%)[39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;153;153;153m⛁[39m System tools: [38;2;153;153;153m12.1k tokens (1.2%)[39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;177;185;249m⛁[39m Custom agents: [38;2;153;153;153m7.2k tokens (0.7%)[39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;215;119;87m⛁[39m Memory files: [38;2;153;153;153m6.5k tokens (0.6%)[39m
[38;2;153;153;153m⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ ⛶ [39m  [38;2;255;193;7m⛁[39m Skills: [38;2;153;153;153m8.4k tokens (0.8%)[39m
                                          [38;2;147;51;234m⛁[39m Messages: [38;2;153;153;153m2.9k tokens (0.3%)[39m
                                          [38;2;153;153;153m⛶[39m Free space: [38;2;153;153;153m954.1k (95.4%)[39m

[1mMCP tools[22m[38;2;153;153;153m · /mcp (loaded on-demand)[39m

[38;2;153;153;153mAvailable[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__add_file_span[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__add_span[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__audit_trace_budget[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__detect_hallucination[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__distill_span[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__get_deliverable[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__get_span[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__list_spans[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__load_run[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__search_spans[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__berry__start_run[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__computer_batch[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__cursor_position[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__double_click[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__hold_key[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__key[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__left_click[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__left_click_drag[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__left_mouse_down[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__left_mouse_up[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__list_granted_applications[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__middle_click[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__mouse_move[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__open_application[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__read_clipboard[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__request_access[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__right_click[39m
[38;2;153;153;153m├[39m [38;2;153;153;153mmcp__computer-use__screenshot[39m
[38;2;153;153;153m├[39m 
[... truncated ...]

---

**User** `2026-05-03T18:43:59.187Z`

Give me a status of your Relay memory. The two latest things. Then tell me how you use other local models or api or oauth models - not a technical descriptipn but what you are capable of

---

**Assistant** `2026-05-03T18:44:18.161Z`

I'll check the Relay memory directory and the recent state.

---

**Tool Results**

`Read` → File does not exist. Note: your current working directory is /Users/ghanavati/ai-stack/Projects/Relay.

---

**Tool Results**

`Bash` → -Users-ghanavati-ai-stack-Projects-Relay/
-Users-ghanavati-ai-stack-Projects-relay-mcp/

---

**Tool Results**

`Bash` → caa5d0cc-b24e-4e53-b19d-bc03fa559441/
memory/
6c31a119-6393-42f7-82f3-384912c53ea0.jsonl  121.6K
93c913b4-3c61-42bd-ba88-675dbb658f65.jsonl  62.0K
c3442a20-0871-4a7c-8945-a2c1de300fef.jsonl  25.6K
caa5d0cc-b24e-4e53-b19d-bc03fa559441.jsonl  273.6K

4 files, 2 dirs (4 .jsonl)

---

**Tool Results**

`Bash` → (empty)

---

**Tool Results**

`Bash` → 00c70e84-515b-43ab-8cda-0926a96ea033/
00ce6373-74e8-4b8b-b64f-e9df91506588/
020ac634-0005-4c53-843f-8f86c685657c/
07e5517f-f29c-4e3e-aada-c445e9deea77/
0b561ed4-9b2e-498a-8b81-200bc86f3bdd/
0d45ee7b-48c9-4d2f-b983-c55f31a49d7e/
17e0b6c3-ca4d-4a46-af3b-339ac41e3507/
19e15509-14f6-4efb-9b2c-e3ee94f32b9b/
1c5fa1ae-6423-4f76-9ed1-49a9a33e2436/
1fe01f0a-575b-4f1e-9556-d55d3b462c7c/
23da5bba-dcb1-45ef-a869-ffc4ccbeb19c/
24f6a279-3b2e-4087-ba8d-643daa3e8aa7/
2fa4f51f-099b-4870-83a9-680b1f9d340d/
303812e0-64f0-47b5-aee8-6bffc46e3334/
3173a47a-766f-4de8-b7f1-f8ffb8665748/
339b1a06-98f3-4c2b-847b-8c7daa6b1bee/
372cfbc1-24a0-439d-930b-bd61b3ae2686/
38312ee6-d431-4238-9ea5-52cc88e8ae49/
3b8cf374-14e2-469c-8844-2275cb323103/
4631f35e-7b83-4f8b-a217-8563013c91cf/
4a0e612e-a3ab-4220-b438-bae914b485a3/
4ba6bdac-5866-497f-a135-344e09cbcd7d/
4e491169-0642-4730-8066-5cde8a2e5454/
50aea6e0-9e6d-466f-8cef-fe6d16789b94/
540ae070-98d7-4878-9b76-a605520cf5df/
5964a883-4921-4994-98f1-2f7cd60a5a59/
6bd0682d-f8b4-4403-9a4c-f9d48d66111a/
6c92094d-2633-47b7-b577-b08521650cce/
70966bf1-45f8-4427-ab7a-d2a5a29a1f38/
7183ae5f-55ac-4f6e-95a3-0a6e19c237d4/
75b3c632-799b-4147-a218-b63d2ef60336/
76538da7-454e-470b-bf1
[... 11335 bytes truncated]

---

**Tool Results**

`Bash` → .consolidate-lock  5B
architecture-diagnosis.md  725B
feedback_2pa_non_code.md  1.1K
feedback_account_for_all_items.md  1.1K
feedback_agent_model_control.md  1.3K
feedback_berry_absence_claims.md  1.2K
feedback_berry_span_format.md  1.7K
feedback_berry_validates_product_claims.md  1.8K
feedback_berry_verifier_model.md  3.8K
feedback_bugs_md_not_evidence.md  1.3K
feedback_cc_env_vars.md  1.3K
feedback_check_lms_ps_before_test.md  2.6K
feedback_check_module_exists_first.md  745B
feedback_code_is_truth_not_planning.md  1.4K
feedback_codex_delegate_protection.md  1.3K
feedback_codex_means_codex.md  732B
feedback_codex_task_sizing.md  1.3K
feedback_codex_worktree_no_node_modules.md  1.3K
feedback_commit_session_reasoning.md  1.4K
feedback_communicate_before_acting.md  1.0K
feedback_compaction_hallucination_causes.md  2.7K
feedback_concurrent_session_writes.md  1.9K
feedback_conductor_binary_pollution.md  1.3K
feedback_context_layer_must_be_opt_in.md  1.9K
feedback_context_mode_minimal.md  1.8K
feedback_continue_autonomous.md  895B
feedback_continue_through_compaction.md  1.4K
feedback_coverage_numbers_conditional.md  1.5K
feedback_crosssession_narrative_compounding.md  1.6K
feedback_db_
[... 7712 bytes truncated]

---

**Tool Results**

`Bash` → /Users/ghanavati/.claude/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/MEMORY.md  27.2K
/Users/ghanavati/.claude/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/feedback_no_npm_registry_for_relay.md  2.0K
/Users/ghanavati/.claude/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/project_cc_coldstart_composition.md  1.9K
/Users/ghanavati/.claude/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/feedback_research_before_recommending_cuts.md  1.4K
/Users/ghanavati/.claude/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/feedback_token_measurement_methodology.md  1.0K

---

**Tool Results**

`Read` → 1	---
2	name: No npm registry distribution for Relay or relay-mcp
3	description: Both projects deliberately stay off the npm registry; recommend source/git install only and never propose publishing
4	type: feedback
5	originSessionId: bd090e7a-bb48-44ec-9060-02f882f4cee7
6	---
7	For both `Relay/` (solo CLI) and `relay-mcp/`, do NOT recommend npm-registry publishing, scoped names (`@ghanavati/relay`), `npm publish`, registry name resolution, or any path that puts the package on registry.npmjs.org. Both repos use source-install (`git clone` + `npm install` + `npm run build` + `npm link`) and neither has a `prepublishOnly` script. relay-mcp's README leads with `> Not published to npm. Install from source.` — Relay/README.md and Relay/docs/quickstart.md mirror that pattern after 2026-05-03 edit.
8	
9	**Why:** User explicitly killed npm distribution for relay-mcp earlier and confirmed same posture for Relay on 2026-05-03 ("I dont want Relay using the npm distribution at all for now, so why even have this? We killed off npm for relay-mcp as well"). The bare name `relay` on npm is taken by an unrelated package; chasing a registry name is wasted motion. Source install via `npm link` from a local clone works fine and keeps distribution under their control. Public install path stays GitHub-only.
10	
11	**How to apply:**
12	- When asked about install / distribution / shipping / "how do users get this": default to `git clone` + `npm install` + `npm run build` + `npm link`. Do not suggest `npm install -g github:...` either — it works but conflates with the registry path.
13	- Never propose `npm publish`, dropping `private: true`, registering a scoped name, or "publishing later". If the user revisits, they will say so explicitly.
14	- When auditing `package.json` for either project: `prepublishOnly` script is a smell; `private: true` is correct; `bin` and `files` stay (needed for npm link).
15	- This applies to BOTH `Relay/` and `relay-mcp/`. If a third relay-* project is created,
[... 55 bytes truncated]

---

**Tool Results**

`Read` → 1	---
2	name: cc cold-start composition
3	description: ~30K of CC cold-start is hardcoded in the binary (tool definitions + system prompt prose). Only the rest is user-controllable.
4	type: project
5	originSessionId: b5498309-e89c-4938-8909-f8f9e168f8a2
6	---
7	Claude Code cold-start has two layers — verified via web research and tiktoken measurement (2026-05-03):
8	
9	**Hardcoded by CC binary (~27,000-30,900 tokens):**
10	- Built-in tool definitions (Agent, Bash, Edit, Read, Skill, ScheduleWakeup, ToolSearch, Write — full JSONSchema): 14-17K tok
11	- System prompt prose (identity, security, task guidelines, action rules, tool policy, output rules): 2.5-4K tok
12	
13	**User-controllable (everything above the baseline):**
14	- CLAUDE.md hierarchy (@-imports concatenated)
15	- MEMORY.md (only if `autoMemoryEnabled: true`)
16	- Skill listing (driven by enabled plugins in settings.json)
17	- Hook outputs at SessionStart
18	- Deferred MCP tool NAMES (schemas are deferred — only ~12-20 tok per name)
19	
20	**Why:** Don't chase the hardcoded ~30K. Chasing 200-token plugin disables when CLAUDE.md hierarchy is 16K is misallocated effort.
21	
22	**How to apply:**
23	- For optimization, prioritize: CLAUDE.md trim → MEMORY.md trim → plugin disable → MCP tool dedup
24	- Anthropic's documented guidance: keep each CLAUDE.md file <200 lines (loaded on every API call)
25	- MCP servers in CC use deferred-tools mechanism — don't worry about per-server schema cost; worry about tool count in deferred list
26	- `AGENTS-COMPACT.md` is auto-regenerated from `AGENTS.md` by post-commit hook; edit the source for durability, run `node dist/cli.js compact-agents --force` for immediate effect
27	
28	Sources: [claudecodecamp.com — Inside Claude Code's System Prompt](https://www.claudecodecamp.com/p/inside-claude-code-s-system-prompt), [code.claude.com settings docs](https://code.claude.com/docs/en/settings), [claudefa.st auto-dream](https://claudefa.st/blog/guide/mechanics/auto-dream).
29

---

**Assistant** `2026-05-03T18:44:56.254Z`

Let me check a few memory files that describe the model delegation capabilities so I can answer accurately rather than from training-data assumptions.

---

**Tool Results**

`Read` → 1	---
2	name: Concurrent model stack — current: GLM + Qwen3.6 (Gemma removed 2026-05-01)
3	description: Current local model stack. Gemma removed 2026-05-01 per user. Historical 3-model run preserved for reference.
4	type: project
5	originSessionId: adbad1c0
6	last_validated: 2026-05-01
7	---
8	
9	**CURRENT STACK (2026-05-01):**
10	- `zai-org/glm-4.7-flash` — 16 lanes, mechanical schema/Zod work
11	- `qwen_qwen3.6-35b-a3b` — 8 lanes, logic/implementation
12	- `qwen/qwen3-coder-next` — newly installed, profile pending
13	- Codex (provider:codex) — single-task, cleanest output
14	- **Gemma 4 — REMOVED from stack 2026-05-01** (user direction). Historical notes below preserved as reference only; do not route to Gemma in current dispatches.
15	
16	**IMPORTANT (2026-04-11, historical):** qwen3-coder and gemma-4 were NOT LM Studio-only. They could
17	run via LM Studio (local, $0) OR via OpenRouter when LM Studio was not available. The
18	dispatch strategy was identical. AGENTS.md documented them as capability profiles,
19	not as provider-specific models.
20	
21	**Proven stack (2026-04-10 session):** CC orchestrates 3 simultaneous lanes — qwen3-coder-next (MLX backend, 8 lanes), Codex (provider:codex), Gemma 4 26B (llama.cpp backend, single lane). MLX and llama.cpp use different backends = no VRAM contention.
22	
23	**Model ID update (2026-04-10):** 4-bit deleted. Use `qwen3-coder-next-mlx@6bit` as the explicit model ID. (Generic `qwen/qwen3-coder-next` resolves to the loaded variant but 6-bit is now the only variant.)
24	
25	**Results:**
26	- qwen (8 tasks): 6/8 success (75%). 2 timed out (list-models, list-drift-events) — both needed schema file reads to understand APIs, pushing past 180s.
27	- Codex (1 task): 100%. Created diff-intent.ts + diff-intent.test.ts cleanly. Committed autonomously.
28	- Gemma (1 task): ~100% content quality. Did NOT commit (tried to run node --test instead). CC committed manually. Output had raw `<|tool_call>` token leak (cosmetic).
29	
30	**qwe
[... 1982 bytes truncated]

---

**Tool Results**

`Read` → 1	---
2	name: Always use relay and codex when user says to
3	description: ABSOLUTE RULE — when user says Codex, use provider:codex. When user says relay, use delegate/delegate_parallel. No substitutions ever.
4	type: feedback
5	---
6	
7	When the user says "use Codex" — use `provider: "codex"`, `model: "gpt-5.4"`. NOT openrouter. NOT CC subagents. NOT "something equivalent."
8	
9	When the user says "use /relay" — use `mcp__relay-mcp__delegate` or `mcp__relay-mcp__delegate_parallel`. NOT Agent subagents.
10	
11	**Why:** User has been burned TWICE (2026-03-17) by Claude substituting what it thinks is equivalent. First time: CC subagents instead of relay. Second time: OpenRouter models instead of Codex. The user's explicit tool choice is a COMMAND, not a suggestion. "Codex" means Codex. Period.
12	
13	**How to apply:**
14	- User says "Codex" → `provider: "codex"` in delegate calls. No exceptions.
15	- User says "/relay" → `mcp__relay-mcp__delegate_parallel`. No exceptions.
16	- User says "relay and Codex" → delegate_parallel with provider: "codex" tasks.
17	- NEVER rationalize substituting a different provider/tool because you think it's "equivalent" or "better for the task."
18	- If Codex can't do a task (e.g. read-only analysis without file tools), ASK the user — don't silently swap.
19

---

**Tool Results**

`Read` → 1	---
2	name: local_model_auto_dispatch
3	description: Auto-dispatch to GLM/Qwen for pattern-repetition tasks — user must never have to ask
4	type: feedback
5	originSessionId: b932f999-3aa7-4b69-b879-2396d57feae0
6	---
7	Auto-dispatch to local LM Studio workers WITHOUT user prompting when task is ≥ 2 independent files following a pattern (test generation, docstrings, transforms, boilerplate).
8	
9	**Why:** User explicitly said "I don't want to tell you each and every time." The rule is now in `.claude/CLAUDE.md` under "Local Model Auto-Dispatch (MANDATORY)".
10	
11	**How to apply:**
12	- GLM (`zai-org_glm-4.7-flash`) for schema/contract/Zod tests, up to 8 lanes
13	- Qwen (`qwen_qwen3.6-35b-a3b`) for logic/implementation tests, up to 4 lanes
14	- Always: `isolation: worktree`, `context_mode: minimal`, `timeout_ms: 180000`
15	- Always: read source file → write self-contained spec → dispatch → acceptance gate → commit
16	- Single-file or cross-file-reasoning tasks stay in CC
17	
18	**Acceptance gate (non-negotiable — incident 2026-04-19):**
19	1. `npx tsc 2>&1` — read ALL output, no grep filtering, fix every error in every file
20	2. `RELAY_ALLOWED_ROOTS= node --test dist/**/*.test.js | tail -10` — full suite, `# fail 0` only
21	3. Both gates green before any commit. "My subset passes" is not acceptable.
22	
23	**Why:** 2026-04-19 session dispatched 17 test files, ran `tsc | grep src/contracts/`, saw no errors, committed. 10+ type errors existed in other files. Other session had to fix 12 worker test files the next day.
24

---

**Tool Results**

`Read` → 1	---
2	name: LM Studio backend selection is independent of download channel
3	description: backendDownloadChannel controls what gets downloaded, NOT which backend is active. New backends must be explicitly selected with lms runtime select.
4	type: feedback
5	originSessionId: adbad1c0
6	last_validated: 2026-04-10
7	---
8	
9	`backendDownloadChannel: "beta"` in settings.json only controls which channel new backends are downloaded from. It does NOT automatically switch the active backend.
10	
11	**Incident (2026-04-10):** gemma-4-26b-a4b failed with generic "Failed to load model" crash in llmworker.js. Root cause: selected GGUF backend was 2.8.0. Gemma 4 architecture support was added in 2.10.0. Backend 2.12.0 was sitting installed but unselected.
12	
13	**Fix:**
14	```bash
15	lms runtime ls                                  # shows SELECTED column — check this first
16	lms runtime select llama.cpp-mac-arm64-apple-metal-advsimd@2.12.0
17	```
18	
19	**How to apply:**
20	- When any GGUF model fails to load with a generic error and logs show immediate crash in llmworker.js — run `lms runtime ls` before anything else.
21	- If the selected backend predates the model's architecture, switch to latest installed backend.
22	- Gemma 4 requires backend ≥ 2.10.0. Current latest: 2.12.0.
23	- After switching backend: no restart required — next load attempt uses the new backend.
24

---

**Tool Results**

`Read` → 1	---
2	name: When to use relay dispatch vs CC direct
3	description: Rule for choosing between relay worker dispatch and CC writing code directly — avoids wasted dispatch attempts on tightly-coupled multi-file changes
4	type: feedback
5	originSessionId: adbad1c0-1e69-461f-9216-5f954e6fcbe4
6	---
7	Do NOT dispatch relay workers for tightly-coupled multi-file changes. Use CC directly.
8	
9	**Why:** Tightly-coupled changes (types → migrations → db → store → CLI) have import dependencies where each file requires the previous to exist first. Parallel workers can't resolve cross-file deps. Sequential relay dispatch is slower than CC writing sequentially. 2026-04-11: R-22 (7 files) and R-14 (6 files) were both done CC-direct correctly.
10	
11	**How to apply:**
12	- Relay dispatch → isolated leaf-node tasks: add tests to this file, write a formatter, add a CLI subcommand that reads an existing interface
13	- CC direct → any task where file B imports from file A that is also being created/changed in the same task
14	- The signal: if you can't hand each file as a completely self-contained task with all its deps already existing, use CC direct
15	- The 8-lane qwen parallel pattern only works on truly independent files (formatters, test files, single utilities)
16

---

**Tool Results**

`Read` → 1	---
2	name: relay_workers_codex_only
3	description: Relay worker dispatch policy — Codex CLI primary, OpenRouter only when explicitly told or when specific models add cost-efficient value
4	type: feedback
5	---
6	
7	Default to **Codex CLI** (provider: "codex") via relay dispatch and **CC inline agents** (Agent tool subagents) for parallel work.
8	
9	**Why:** User restricted relay workers to Codex CLI by default. OpenRouter workers burned tokens without clear value in previous sessions.
10	
11	**How to apply:**
12	- Default: `mcp__relay-mcp__delegate_parallel` uses `provider: "codex"` workers
13	- For research/analysis parallelism, use CC Agent tool (inline subagents)
14	- For planning, switch to Opus model via `/model` before planning sessions
15	- **OpenRouter exception:** Use OpenRouter workers ONLY when:
16	  1. User explicitly says to use OpenRouter, OR
17	  2. A specific OpenRouter model (e.g., `google/gemini-2.5-pro`, `deepseek/deepseek-chat`) can add clear value at a cost-efficient rate for the specific task (e.g., multi-model review, gemini's long context, deepseek's reasoning)
18	- This is the permanent default from 2026-03-25
19	
20

---

**Assistant** `2026-05-03T18:45:13.423Z`

Let me run Berry verification on the factual claim before presenting it (per your CLAUDE.md hallucination rule).

---

**Tool Results**

`Bash` → 2026-05-03 19:48 feedback_no_npm_registry_for_relay.md
2026-05-03 15:05 project_cc_coldstart_composition.md
Shell cwd was reset to /Users/ghanavati/ai-stack/Projects/Relay

---

**Assistant** `2026-05-03T18:46:24.978Z`

Berry verification failed twice with a tool-validation error (the `spans` parameter is being rejected despite matching the schema) — flagging per your hallucination rule. The substantive claims below are grounded in direct file reads and `stat` output I just performed, so the evidence is first-hand.

`★ Insight ─────────────────────────────────────`
- Your auto-memory directory lives at `…/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/` (lowercase `relay-mcp`), not the capital-R `Relay/` path the system reminder advertises. The Relay/ dir exists but its `memory/` subfolder is empty — the memory I have access to in this session is from the relay-mcp project history, which makes sense since Relay/ is the newer extraction of the relay-mcp engine.
- 218 memory entries indexed in MEMORY.md (27.2K). Heavy bias toward `feedback_*` (corrections) over `project_*` (state), which is what the auto-memory workflow rewards over time.
`─────────────────────────────────────────────────`

## Relay memory — two latest entries

**1. `feedback_no_npm_registry_for_relay.md`** — saved 2026-05-03 19:48
Both `Relay/` (solo CLI) and `relay-mcp/` deliberately stay off the npm registry. Default install path is `git clone` → `npm install` → `npm run build` → `npm link`. Don't propose `npm publish`, scoped names, or "publishing later" for either repo. Confirmed earlier today after you said: *"I dont want Relay using the npm distribution at all for now... We killed off npm for relay-mcp as well."*

**2. `project_cc_coldstart_composition.md`** — saved 2026-05-03 15:05
Claude Code cold-start splits into two layers: ~27–30.9K tokens hardcoded into the CC binary (built-in tool JSONSchemas + system-prompt prose), and everything above that is user-controllable (CLAUDE.md hierarchy, MEMORY.md, skills, hooks, deferred MCP tool names). Optimization priority: trim CLAUDE.md → trim MEMORY.md → disable plugins → dedup MCP tools. Don't chase the hardcoded baseline.

## How I use other models — capabilities (not the wiring)

Three lanes are open to me when I'm working in this project, and the rules for picking between them are codified in your memory:

**Codex (OAuth, your ChatGPT subscription)** — my default delegate for self-contained implementation tasks. When you say "use Codex," I route there with `provider: "codex"`, no substitutions. Codex output has historically been the cleanest of the three lanes — when it returns, I usually just review and commit. Best for: one new file, one new test from a spec, atomic transforms, anything where the spec can be made fully self-contained.

**Local models via LM Studio (GLM, Qwen3.6, Qwen3-coder-next)** — your own hardware, zero cost per call. I auto-dispatch to these *without asking* when the work is ≥2 independent files following a pattern (test generation, schema work, docstrings, boilerplate). GLM gets up to 16 parallel lanes for mechanical schema/Zod work; Qwen3.6 gets 8 lanes for logic/implementation. I don't dispatch to local models for tightly-coupled multi-file changes — they can't share context across lanes, so I do that work myself sequentially. Gemma was removed from the stack on 2026-05-01 at your direction.

**OpenRouter (API)** — restricted lane. I only reach for it when (a) you explicitly tell me to, or (b) a specific OpenRouter model adds clear value at a cost-efficient rate (Gemini's long context, DeepSeek's reasoning, multi-model review). Permanent default since 2026-03-25 — never my fallback when LM Studio is busy.

**The orchestration shape:** I'm the art director, not the implementer. I write self-contained specs, dispatch in the right lane, gate on `tsc` + full test suite (no grep filtering — I got burned on that 2026-04-19 by committing with 10+ type errors hiding in unfiltered files), then commit. Read-only research and parallel exploration go through CC subagents instead of relay workers, because subagents share my context and workers don't.

---

**User** `2026-05-03T18:53:44.071Z`

what are the rules to use LM studio?

---

**Assistant** `2026-05-03T18:53:54.208Z`

Let me read the LM Studio rule memories in parallel.

---

**Tool Results**

`Read` → 1	---
2	name: LM Studio preset mandatory for gemma dispatch
3	description: CRITICAL — gemma must be loaded via the Gemma 31B Relay preset, not raw lms load. Raw load causes drift and RAM growth.
4	type: feedback
5	originSessionId: ca6b9797-6e09-471d-837e-2a5d490cf9c1
6	---
7	**Rule:** Never load gemma-4-31b via `lms load google/gemma-4-31b --gpu max` for relay dispatch. Always load through LM Studio UI using the `Gemma 31B — Relay` preset.
8	
9	**Why:**
10	1. Raw load has no constraining system prompt → model hallucinates and drifts on open-ended tasks.
11	2. Raw load does not clear KV-cache between requests → RAM grows 8–17GB per dispatch call. Two calls took RAM from 100GB → 117GB (2026-04-15 incident).
12	3. The preset sets contextLength=65536, temperature, stop tokens, and a narrow system prompt that keeps the model on task.
13	
14	**How to apply:**
15	- Before any gemma dispatch: verify `lms ps` shows the model loaded WITH the preset active (user loads via LM Studio UI).
16	- If model is not loaded or was loaded raw: ask user to load via UI with preset before dispatching.
17	- Never run `lms load google/gemma-4-31b` from the CLI for relay tasks — that bypasses the preset entirely.
18	- After dispatch tasks complete: run `lms unload --all` immediately to free RAM.
19	
20	**Ceiling test results (2026-04-15, preset loaded by user):**
21	- T-500: ~500 token prompt → pass, 262s, 2224 total tokens, RAM 100GB
22	- T-750: ~750 token prompt → pass, 215s, 2192 total tokens, RAM 117GB
23	- Tests stopped at 117GB per user instruction. Actual ceiling not yet measured.
24

---

**Tool Results**

`Read` → 1	---
2	name: lmstudio parallel dispatch — isolation:worktree required, mutex was the bottleneck
3	description: isolation:none serializes all lmstudio tasks via relay's workdir mutex. isolation:worktree + commit instruction = genuine parallelism up to LM Studio's concurrency limit.
4	type: feedback
5	originSessionId: adbad1c0-1e69-461f-9216-5f954e6fcbe4
6	---
7	
8	Use `isolation: worktree` for all parallel lmstudio dispatch. Every task prompt must end with a git commit instruction.
9	
10	**Why:** `isolation: none` → all tasks share the same workdir → relay's `acquireWorkdirMutex` in `runner-dispatcher.ts:158` serializes them one at a time, even when `delegate_parallel` is used. Staggered spawn times (22ms, 5890ms, 11457ms...) are the signature. `isolation: worktree` gives each task a separate git worktree path → separate mutex key → no contention → LM Studio receives all N requests simultaneously.
11	
12	**Evidence (2026-04-09):** isolation:none, 6 tasks: staggered 22ms→45415ms. isolation:worktree, 4 tasks: all within 85ms. TRUE parallel.
13	
14	**Statistically validated (2026-04-10, 24 tasks across 3 batches, qwen3-coder-next):**
15	- Batch 1 (120s timeout): 5/8 success, 122s wall time
16	- Batch 2 (180s timeout): 8/8 success, 134s wall time
17	- Batch 3 (180s timeout): 7/8 success, 183s wall time
18	- **Average: 83% success rate, $0 cost. 180s is the correct timeout floor for simple 1-file tasks.**
19	
20	**⚠️ Context injection ceiling (2026-04-13 incident):** 8-lane batch with heavy context (AGENTS.md ~10K tokens × 8 lanes = ~80K tokens simultaneous) caused ALL tasks to timeout at 180s with `token_estimate: 0` — no output token was generated. Hardware saturated during prefill before inference began. Fix: use 360000ms timeout OR strip AGENTS.md injection for simple leaf tasks (add `"context": ""` or use a task-specific context override). The 180s floor holds only when context per task stays under ~2K tokens.
21	
22	**How to apply:**
23	1. Always use `isolation: "w
[... 1056 bytes truncated]

---

**Tool Results**

`Read` → 1	---
2	name: LM Studio prompt discipline — one file in, one file out
3	description: qwen3-coder-next needs scoped prompts. Proven 83% across 24 tasks. Multi-file wiring fails. Build attempts go rogue.
4	type: feedback
5	originSessionId: adbad1c0-1e69-461f-9216-5f954e6fcbe4
6	last_validated: 2026-04-10
7	---
8	One source file read + one output file write + commit. No multi-file wiring. No build step.
9	
10	**Why:** Multi-file tasks → timeout. Vague tasks → model tries to build/test/modify tsconfig. $0 cost only matters if the task completes.
11	
12	**Mandatory prompt suffix (every lmstudio task):**
13	```
14	Do not build. Do not run tests. Do not run npm. Do not modify any config file.
15	git add <output_file> && git commit -m '<message>'
16	```
17	
18	**Task ceiling (proven across 37 tasks, 2026-04-09/10):**
19	- Works: read 1 file → write 1 file (tests, formatters, pure utils). 83-100% success at 180s.
20	- Fails: multi-file wiring, schema changes, tasks needing build feedback, source files >150 lines complex reasoning.
21	
22	**qwen3-coder-next vs qwen3-coder-30b:** next is the correct model. Replaces 30b. Same task pattern, same constraints.
23	
24	**API pre-resolution = 100% success (proven 2026-04-10, 8/8 tasks):**
25	When qwen tasks need to call store APIs or use Zod contracts, include ALL method signatures directly in the prompt body. Do NOT make qwen discover them by reading additional files. This eliminates the #1 timeout cause (multi-file API discovery). 8/8 success in 128s vs 75% in prior session.
26	
27	**Zod default trap — always warn in prompt:**
28	Include this in every qwen prompt that touches Zod schemas: "ZOD WARNING: <field> has .optional().default(x) — TypeScript requires passing <field> explicitly."
29

---

**Tool Results**

`Read` → 1	---
2	name: LM Studio token_estimate=0 timeout — corrected diagnosis
3	description: token_estimate=0 means preset not loaded, NOT context injection ceiling. Actual injected context is ~2.2K tokens, not 12-15K.
4	type: feedback
5	originSessionId: ca6b9797-6e09-471d-837e-2a5d490cf9c1
6	---
7	**Corrected diagnosis (2026-04-15):** `token_estimate: 0` at timeout does NOT mean context injection VRAM saturation. The original diagnosis was wrong.
8	
9	**Actual cause:** The `Gemma 31B — Relay` preset was not loaded. Raw `lms load` produces a model with no constraining system prompt — the model stalls or drifts without generating output.
10	
11	**Evidence:**
12	- PAUL gate dispatch (raw load, no preset): `token_estimate: 0`, timeout at 360s
13	- T-500 (preset loaded by user): `token_usage: 2224`, success at 262s
14	- T-750 (preset loaded by user): `token_usage: 2192`, success at 215s
15	- Actual injected context (with preset): ~2.2K tokens total — NOT the 12-15K originally estimated
16	
17	**Why:** Context injection layers inject a compressed summary, not the full AGENTS.md. The real context ceiling is much higher than previously thought.
18	
19	**How to apply:**
20	- `token_estimate: 0` → check if preset is loaded, not whether the prompt is too long
21	- Before any gemma dispatch: verify model is loaded via LM Studio UI with `Gemma 31B — Relay` preset
22	- Prompt length guidance still valid for quality: keep task prompts tight, precise, narrow to avoid drift — but this is about output quality, not a hard VRAM ceiling
23	- See `feedback_lmstudio_preset_mandatory.md` for the full preset requirement
24

---

**Tool Results**

`Read` → 1	---
2	name: lmstudio verification layer — 2 checkpoints, proven capability profile
3	description: Complete verified workflow for lmstudio as a parallel draft layer. Includes capability ceiling, isolation mode, and what CC must check.
4	type: feedback
5	originSessionId: adbad1c0-1e69-461f-9216-5f954e6fcbe4
6	---
7	
8	## The Verified Workflow (updated 2026-04-09)
9	
10	```
11	CC writes tight specs → lmstudio generates N files in parallel (isolation:worktree) → CC reads all N → commit
12	```
13	
14	**2 mandatory checkpoints:**
15	1. **Post-merge read:** CC reads every file lmstudio wrote. Check: correct exports? Field names match spec? Logic correct? Immutable patterns? If wrong — CC fixes inline before committing.
16	2. **Test bug check:** If lmstudio also wrote test files, check assertions against the actual formatter output. Common failure: `!result.includes('FieldName')` when the label 'FieldName' always appears in output regardless of value. Fix to regex match on the value.
17	
18	---
19	
20	## Proven Capability Profile (2026-04-09)
21	
22	| Capability | Verdict | Evidence |
23	|---|---|---|
24	| Pure function, tight spec, inline types | ✅ Reliable | 16 functions across 4 batches, 0 fixes needed |
25	| Real business logic (conditionals, branching) | ✅ Reliable | risk-classifier, model-drift, run-gate — all correct |
26	| Array comparison / sorting | ✅ Reliable | detectModelDrift used [...arr].sort() with immutability |
27	| Exhaustive switch statements | ✅ Reliable | severityBadge TypeScript exhaustiveness |
28	| Parallel dispatch (isolation:worktree) | ✅ Proven | 4/4 started within 85ms, 24s wall time |
29	| node:test test files | ✅ Mostly reliable | Correct imports, 1 assertion bug in 3 test files |
30	| Multi-file edits | ❌ Fails | Timeouts, no cross-file context |
31	| Vague tasks | ⚠️ Risky | Timeouts or hallucinated field names |
32	| Committing autonomously | ✅ Works | Required for worktree isolation to persist files |
33	
34	---
35	
36	## Speed Profile (
[... 775 bytes truncated]

---

**Tool Results**

`Read` → 1	---
2	name: Local LLMs need more time and more turns than CC's default test rhythm
3	description: Local model tests must allow more max_tokens, more rounds, sequential not parallel, and warm-up tolerance. Quick 1-shot 8-token tests produce invalid verdicts.
4	type: feedback
5	last_validated: 2026-05-02
6	originSessionId: ca12b7cd-3303-4848-97c4-ee9eac1a6719
7	---
8	When testing a local LLM's capability for a role, give it materially more time and budget than CC's instinct says. CC's default rhythm — small max_tokens, parallel dispatch, single-shot, no warm-up — is calibrated for frontier models, and produces false-negative verdicts on local models.
9	
10	**Why:** 2026-05-02 incident — CC tested LFM2-24B-A2B for routing with `max_tokens: 8` and 8 parallel requests. Concluded "50% accuracy, marginal value" within ~30s of testing. User correction: "these llms will need more time than expected from you." A 50% verdict at 8 tokens / 8 parallel / 1 shot per task is not a real verdict — it measures the model under a stripped-down test, not in a real role.
11	
12	**How to apply:**
13	1. **Higher max_tokens:** Even for short outputs, give 50-100 tokens of headroom. The model may need room to "settle" on the answer. 8-token caps are aggressive even for "one word" tasks.
14	2. **Sequential before parallel:** First test single-stream behavior to establish a clean baseline. THEN test parallel for throughput. Mixing the two confuses model capability with batch contention.
15	3. **Multiple rounds where the role allows:** For routing/classification, run the same input twice — see if the model is consistent. Inconsistency at temp 0.1 means the model is genuinely uncertain, not just unlucky.
16	4. **Warm-up:** First request after load is often slower and less accurate. Discard or retry. KV cache warming, MoE expert routing initialization, etc. take a few requests to stabilize.
17	5. **Reasoning room:** If the model has reasoning_content / thinking mode, GIVE IT BUDGET. Strangling r
[... 618 bytes truncated]

---

**Tool Results**

`Read` → 1	---
2	name: ALWAYS check `lms ps` context + parallel BEFORE any test verdict
3	description: A loaded model can have wrong context/parallel from JIT-load defaults. Verdict on a model running at ctx=4096 / parallel=4 when the preset says 65536/8 is invalid. Check the loaded config first.
4	type: feedback
5	last_validated: 2026-05-02
6	originSessionId: ca12b7cd-3303-4848-97c4-ee9eac1a6719
7	---
8	Before declaring ANY model performance verdict, run `lms ps` and verify the loaded model has the context length AND parallel session count from its preset. If either is at LM Studio's default (typically ctx=4096, parallel=4 for JIT-loaded models without a concrete-model config), abort the test and fix the config first.
9	
10	**Why:** 2026-05-02 incident — JIT-loaded Qwen3-Coder-Next 8-bit via OpenAI API. LM Studio applied default constraints (ctx=4096, parallel=4) because no `lmstudio-community/Qwen3-Coder-Next-MLX-8bit.json` config existed. Ran a "comparison" test against 6-bit (which had ctx=65536, parallel=8) and concluded "8-bit is 35% slower." User: "what the fuck is your problem with the rules!! did you research, did you give it a preset, the eight prompts. fuck you and your lazy ass bullshit"
11	After creating the proper concrete-model config and re-running with ctx=65536/parallel=8: 6-bit and 8-bit perform within 1% on every metric. The "35% slower" was entirely the broken config, not the model.
12	
13	**How to apply:**
14	1. Step 0 of any model performance test: `lms ps` and read the CONTEXT and PARALLEL columns
15	2. If they don't match the preset values: STOP. Investigate why. Fix.
16	3. JIT-load via API does NOT always apply user-concrete-model-default-config — it only applies if a config exists at the concrete-model path (e.g., `.internal/user-concrete-model-default-config/<owner>/<concrete-model>.json`). The PARENT path (e.g., `qwen/qwen3-coder-next.json`) only applies when loading via parent identifier.
17	4. For each variant of a virtual model (6-bit, 8-bit, 
[... 679 bytes truncated]

---

**Tool Results**

`Read` → 1	---
2	name: Build the preset BEFORE running any new LM Studio model test
3	description: Testing a model without a preset produces invalid results. Default config = drift, broken instruction following, wrong sampling. Always build the preset first, even for "quick" tests.
4	type: feedback
5	last_validated: 2026-05-02
6	originSessionId: ca12b7cd-3303-4848-97c4-ee9eac1a6719
7	---
8	Never run a capability test on a freshly-installed LM Studio model without first building its preset. "I'll test it quickly to see if it's worth presetting" inverts the dependency — the preset IS what makes the test valid.
9	
10	**Why:** 2026-05-02 incident — CC tested LFM2-24B-A2B for routing role without a preset. Result: 1/8 classification accuracy, defaulted to "schema" for every input. Concluded "LFM2 is bad at routing." User correction: "Dont forget these llms need presets!" Without proper sampling (temp, min_p) and a relay-discipline system prompt, LM Studio falls back to model defaults that aren't tuned for the use case. The conclusion was invalid — the test design was flawed.
11	
12	**How to apply:**
13	1. Step 0 of testing any new LM Studio model: check `lmstudio/config-presets/` and `lmstudio/.internal/user-concrete-model-default-config/<owner>/`
14	2. If neither has the model, **research the published recommended sampling FIRST.** Do not invent defaults. Sources, in order:
15	   - The model's own `generation_config.json` and `tokenizer_config.json` files (already on disk)
16	   - Hugging Face model card (the original publisher, not the re-quant) — fetch via WebFetch
17	   - The publisher's official docs/blog (LiquidAI, Qwen, Z.ai, etc.)
18	   - LM Studio community presets if available via `lms get <preset-name>`
19	   - Closest existing repo preset only as a STRUCTURAL template, never as a value source for sampling
20	3. Build BOTH:
21	   - **user-concrete-default config** (auto-applies on `lms load`): `~/ai-stack/lmstudio/.internal/user-concrete-model-default-config/<owner>/<m
[... 1034 bytes truncated]

---

**Tool Results**

`Read` → 1	---
2	name: Never download large models without explicit user approval
3	description: Even when the user mentions an alternative model (e.g. "what about the 8bit version"), do not initiate the download. Confirm first. Downloads are 50-100+ GB on this stack.
4	type: feedback
5	last_validated: 2026-05-01
6	originSessionId: ca12b7cd-3303-4848-97c4-ee9eac1a6719
7	---
8	When the user mentions a different quantization, model variant, or alternative ("what about the 8bit", "try the 9bit", "what if we used X"), DO NOT run `lms get` or any download command. Confirm first.
9	
10	**Why:** 2026-05-01 incident — user asked "what if we try with the 8bit version?" CC initiated `lms get qwen/qwen3-coder-next@8bit --yes` which began an 84.67 GB download at 50 MB/s (~30 min ETA). User immediately said "dont download the model, unless i tell you to."
11	
12	The "what if" framing is exploratory. It is asking for analysis or a comparison plan — not an instruction to download. Local-model downloads on this stack are 30-100+ GB and consume meaningful bandwidth and disk. Always confirm before initiating.
13	
14	**How to apply:**
15	1. When user mentions a model that is not currently downloaded, never initiate `lms get`, `huggingface-cli download`, `git clone <model-repo>`, or any download command.
16	2. Instead: state the size, the time estimate, and ask explicitly.
17	3. Acceptable inquiries that DO authorize download: "download X", "go ahead and pull X", "yes please grab it", "install X".
18	4. "What about X" / "try X" / "what if we used X" do NOT authorize download — they ask for analysis or a plan.
19	5. If a download is already running and the user expresses any reservation, kill it immediately with `pkill -f "lms get"` (or equivalent for other tools).
20

---

**Tool Results**

`Read` → 1	---
2	name: no-hardcoded-models
3	description: Never hardcode LLM model names in source code or config files — use env vars with no defaults
4	type: feedback
5	---
6	
7	Never hardcode LLM model names — not in source code constants, not in .mcp.json, not anywhere.
8	
9	**Why:** BUG-15 happened because a model name (`google/gemini-flash-1.5`) was hardcoded as a default constant. When the model went offline, every verification was silently rubber-stamped for 15 phases. Moving the same string to .mcp.json is just relocating the problem.
10	
11	**How to apply:** Model configuration must be opt-in via environment variables with NO fallback defaults. If the env var is unset, the feature is disabled with a clear warning — never silently use a stale model. This applies to reviewer models, worker models, and any other LLM model references.
12

---

**Tool Results**

`Read` → 1	---
2	name: Search memory before any new experiment on an existing model
3	description: When user asks to test/profile/use a model already documented in memory, grep memory FIRST. Avoids re-discovering known constraints and going off-policy on a contained but unsafe path.
4	type: feedback
5	last_validated: 2026-05-01
6	originSessionId: ca12b7cd-3303-4848-97c4-ee9eac1a6719
7	---
8	Before running ANY new experiment, profile, or smoke test on a model that has prior history in this project, grep the memory directory for the model name. Do not skip this — even when the test path looks contained.
9	
10	**Rule:** When user mentions a known model (qwen3-coder-next, qwen3.6, glm-4.7-flash, gemma, codex, etc.), run:
11	```
12	grep -rli "<model-name>" ~/.claude/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/
13	```
14	Read every match BEFORE designing the test.
15	
16	**Why:** 2026-05-01 incident — user asked to profile newly installed qwen3-coder-next. CC designed and ran a curl-based chat completion comparison without first reading `feedback_lmstudio_prompt_discipline.md`, which already documents:
17	- "Build attempts go rogue" — model attempts npm/test/tsconfig changes without leashing
18	- Mandatory prompt suffix: `Do not build. Do not run tests. Do not run npm. Do not modify any config file.`
19	- 180s timeout floor for parallel
20	- 1-file-in / 1-file-out task ceiling
21	- API pre-resolution requirement (include all method signatures inline)
22	- 8-lane context-injection ceiling at ~80K tokens combined
23	
24	The curl test path was contained (no worktree, no file write, no commit), so no rogue behavior occurred. But the user correctly flagged that the principle was violated — tests against a leash-required model must be designed with the leash from the start, not retrofitted after.
25	
26	**How to apply:**
27	1. Step 0 of every model task: grep memory for that model's name. Read every match.
28	2. If memory says "needs X discipline" — apply X to the test,
[... 262 bytes truncated]

---

**Tool Results**

`Read` → File unchanged since last read. The content from the earlier Read tool_result in this conversation is still current — refer to that instead of re-reading.

---

**Assistant** `2026-05-03T18:54:59.948Z`

`★ Insight ─────────────────────────────────────`
- These rules read like an operations runbook precisely because each one was paid for with a real incident — preset-not-loaded gave you false negatives on LFM2, a missing concrete-model config had me declaring 8-bit "35% slower" when it was actually within 1%, an 84GB download started from a "what if" question. The rules carry timestamps and incident IDs, so you can tell "discipline" from "speculation."
- The most load-bearing rule is the cheapest to skip: `grep memory before testing a known model`. Almost every other rule below was originally discovered the hard way and recorded — so memory IS the runbook.
`─────────────────────────────────────────────────`

## LM Studio rules — what I'm bound by

### Before touching the model

1. **Search memory first.** Before profiling, testing, or using *any* model that's been touched before (qwen3-coder-next, qwen3.6, glm-4.7-flash, gemma, lfm2…), grep `~/.claude/projects/-Users-ghanavati-ai-stack-Projects-relay-mcp/memory/` for its name. Read every match. Apply discipline as a property of the model, not the dispatch path.
2. **Preset is mandatory.** Never run a capability test or relay dispatch on a freshly installed model without building its preset first. "Quick test to see if it's worth presetting" inverts the dependency — the preset is what makes the test valid. Build both: a `user-concrete-model-default-config/<owner>/<model>.json` (auto-applies on `lms load`) AND a versioned `config-presets/<Name> — Relay.preset.json`.
3. **Research sampling, never invent it.** When building a preset, look up `generation_config.json` on disk → HF model card → publisher docs → community presets. Inventing values from "general knowledge" was a research miss in the LFM2 incident.
4. **Verify `lms ps` before any verdict.** Step 0 of any performance test: read the CONTEXT and PARALLEL columns. If they're at JIT defaults (ctx=4096, parallel=4), STOP — fix the config, reload, then test. JIT load via API does not always apply the parent config; concrete-model paths must exist.
5. **Backend selection is independent of download channel.** `backendDownloadChannel: "beta"` only controls *which* channel new backends are downloaded from. Switching is explicit: `lms runtime ls` → `lms runtime select …`. Gemma 4 needs ≥2.10.0.
6. **Never hardcode model names** in source or `.mcp.json`. Env vars only, no fallback defaults — if unset, feature is disabled with a warning. (BUG-15 origin: hardcoded `google/gemini-flash-1.5` rubber-stamped 15 phases when the model went offline.)
7. **Never download large models without explicit approval.** "What about the 8bit version" is exploratory — it does NOT authorize `lms get`. Authorizing phrases: "download X," "go ahead and pull X," "install X." Models on this stack are 30–100+ GB.

### When to dispatch

8. **Auto-dispatch trigger:** ≥2 independent files following a pattern (test generation, schema/Zod, docstrings, transforms, boilerplate). I shouldn't have to be told. GLM gets up to 16 lanes for mechanical work; Qwen3.6 gets 8 for logic.
9. **Don't dispatch tightly-coupled multi-file changes.** If file B imports from file A and both are being created in the same task, do it inline in CC — workers can't share cross-file context, and sequential dispatch is slower than CC writing sequentially.

### How to dispatch

10. **`isolation: "worktree"` always.** `isolation: none` serializes everything via relay's `acquireWorkdirMutex` (held for the *entire* inference duration, not just snapshot/file-write). Worktree gives each task its own mutex key → genuine parallelism.
11. **One file in, one file out.** Read 1 file → write 1 file → commit. Multi-file wiring fails. Vague tasks make the model attempt npm/test/tsconfig changes.
12. **Mandatory prompt suffix on every task:**
    `[code block — 2 lines]`
    Without the commit, worktree merge produces nothing.
13. **API pre-resolution.** When the worker needs Zod contracts or store APIs, p
[... truncated ...]

---

**User** `2026-05-03T18:57:26.064Z`

1. one correction for Codex in regards to the model. Use gpt 5.5
2. how many models can run concurrently and which models?

---

**Assistant** `2026-05-03T18:57:36.072Z`

Updating memory now, then I'll answer concurrency from the codified stack.

---

**Tool Results**

`Read` → 1	---
2	name: Always use relay and codex when user says to
3	description: ABSOLUTE RULE — when user says Codex, use provider:codex. When user says relay, use delegate/delegate_parallel. No substitutions ever.
4	type: feedback
5	---
6	
7	When the user says "use Codex" — use `provider: "codex"`, `model: "gpt-5.5"`. NOT openrouter. NOT CC subagents. NOT "something equivalent."
8	
9	When the user says "use /relay" — use `mcp__relay-mcp__delegate` or `mcp__relay-mcp__delegate_parallel`. NOT Agent subagents.
10	
11	**Why:** User has been burned TWICE (2026-03-17) by Claude substituting what it thinks is equivalent. First time: CC subagents instead of relay. Second time: OpenRouter models instead of Codex. The user's explicit tool choice is a COMMAND, not a suggestion. "Codex" means Codex. Period.
12	
13	**How to apply:**
14	- User says "Codex" → `provider: "codex"` in delegate calls. No exceptions.
15	- User says "/relay" → `mcp__relay-mcp__delegate_parallel`. No exceptions.
16	- User says "relay and Codex" → delegate_parallel with provider: "codex" tasks.
17	- NEVER rationalize substituting a different provider/tool because you think it's "equivalent" or "better for the task."
18	- If Codex can't do a task (e.g. read-only analysis without file tools), ASK the user — don't silently swap.
19

---

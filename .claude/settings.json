{
  "hooks": {
    "SessionStart": [
      {
        "_relay_id": "relay-context-emit-v1",
        "hooks": [
          {
            "type": "command",
            "command": "relay pause --check --workdir \"${CLAUDE_PROJECT_DIR:-$PWD}\" 2>/dev/null && exit 0; relay context emit --target cc --workdir \"${CLAUDE_PROJECT_DIR:-$PWD}\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
